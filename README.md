# 自动部署到 Netlify

这是一个配置了**全自动部署**功能的网站项目。推送代码即可自动部署，无需任何手动操作！

## 🤖 全自动部署特性

- ✅ **零手动操作** - 推送代码自动触发部署
- ✅ **GitHub Actions** - 自动化 CI/CD 流程
- ✅ **Pull Request 预览** - 自动生成预览链接
- ✅ **部署状态通知** - 自动在 commit 中添加状态
- ✅ **快速部署** - 通常 1-2 分钟完成

## ⚡ 快速开始

### 使用一键配置脚本（推荐）

**Windows:**
```bash
一键配置.bat
```

**macOS/Linux:**
```bash
chmod +x 一键配置.sh
./一键配置.sh
```

脚本会引导您完成所有配置步骤。

### 手动配置

详细步骤请查看 **[自动部署配置指南.md](自动部署配置指南.md)**

## 🚀 部署方法

### 方法一：通过 Netlify 网站（推荐）

1. **创建 Git 仓库**
   ```bash
   git init
   git add .
   git commit -m "初始提交"
   ```

2. **推送到 GitHub/GitLab/Bitbucket**
   ```bash
   # 在 GitHub 创建新仓库后
   git remote add origin https://github.com/你的用户名/你的仓库名.git
   git branch -M main
   git push -u origin main
   ```

3. **连接到 Netlify**
   - 访问 [Netlify](https://netlify.com)
   - 点击 "Add new site" → "Import an existing project"
   - 选择你的 Git 提供商（GitHub/GitLab/Bitbucket）
   - 选择你的仓库
   - Netlify 会自动检测 `netlify.toml` 配置文件
   - 点击 "Deploy site"

4. **自动部署已启用！**
   - 每次推送代码到主分支，Netlify 会自动构建和部署
   - 你会获得一个 `xxx.netlify.app` 域名

### 方法二：使用 Netlify CLI

1. **安装 Netlify CLI**
   ```bash
   npm install -g netlify-cli
   ```

2. **登录**
   ```bash
   netlify login
   ```

3. **初始化和部署**
   ```bash
   netlify init
   netlify deploy --prod
   ```

### 方法三：手动拖放部署

1. 访问 [Netlify Drop](https://app.netlify.com/drop)
2. 将整个项目文件夹拖放到页面上
3. 立即获得一个可访问的网站

## 📁 项目结构

```
.
├── index.html          # 主页面
├── style.css           # 样式文件
├── script.js           # JavaScript 文件
├── netlify.toml        # Netlify 配置文件
└── README.md           # 说明文档
```

## ⚙️ 配置说明

`netlify.toml` 文件包含了：
- 发布目录配置
- 重定向规则（SPA 路由支持）
- HTTP 头部配置（安全性和缓存）
- 404 错误页面处理

## 🔧 自定义域名

部署后，你可以在 Netlify 控制台中：
1. 进入 "Domain settings"
2. 添加自定义域名
3. 按照说明配置 DNS

## 🌟 功能特性

- ✅ 自动部署
- ✅ 持续集成
- ✅ HTTPS 自动配置
- ✅ 全球 CDN 加速
- ✅ 免费托管
- ✅ 自动缓存优化
- ✅ 404 页面处理
- ✅ 安全头部配置

## 📝 更新网站

1. 修改代码
2. 提交更改：
   ```bash
   git add .
   git commit -m "更新描述"
   git push
   ```
3. Netlify 会自动检测并重新部署！

## 🔗 有用的链接

- [Netlify 文档](https://docs.netlify.com/)
- [Netlify 配置参考](https://docs.netlify.com/configure-builds/file-based-configuration/)
- [Netlify CLI 文档](https://docs.netlify.com/cli/get-started/)

## 💡 提示

- 每次 `git push` 都会触发自动部署
- 部署历史可以在 Netlify 控制台查看
- 可以回滚到任何之前的部署版本
- 支持预览部署（Pull Request）

